# AGENTS

This project file is additive to the Global `AGENTS.md` instructions in `~/.codex/AGENTS.md` that apply in the workspace. If there is any conflict, follow the more specific instruction.

## Project Continuity

Keep `MEMORY.md` as a concise current project overview. The project's
`.continuity` entry, maintained by the configured global hooks, retains
detailed immutable task checkpoints; do not replace it with local tracker
files or manual session logs. Follow the global continuity procedure and
runtime memory-write policy.
